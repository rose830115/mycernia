# internal build tooling and docs — not part of the public demo
build_data.py
README.md

# macOS cruft
.DS_Store
**/.DS_Store

# original brand source files (only the trimmed lowercase copies are used)
assets/brand/YCM_*.png
